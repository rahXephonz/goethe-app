DATABASE_URL=postgres://goethepro:goethepro@localhost:5432/goethepro
NODE_ENV=development
PORT=8787
# Absolute origin of the Hono API. Used by the web app's SSR/middleware for
# server-to-server auth checks (the web app no longer touches the DB directly).
API_URL=http://localhost:8787
# LLM keys (Tier-2 batch + Tier-3 grading) — add when wiring packages/prompts
# ANTHROPIC_API_KEY=
# OPENROUTER_API_KEY=
LANGUAGETOOL_URL=http://localhost:8010/v2
