# astro's `check` CLI resolves @astrojs/check + typescript from its own location
# in the pnpm store; hoist them to the root so the resolution succeeds.
public-hoist-pattern[]=@astrojs/check
public-hoist-pattern[]=typescript
